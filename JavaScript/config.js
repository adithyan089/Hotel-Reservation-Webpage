const config = {
    logoutPage: {
        title: 'Logout',
        message: 'Are you sure you want to logout?',
        redirectUrl: '/login',
        timeout: 5000 // milliseconds
    }
};

module.exports = config;